// Send Response message auto reply
export default async function ({ data }) {

    // console.log(data);
    // const io = request.app.get('io');
    // io.to(data.room).emit('message', {
    //     ...data,
    // });

}
